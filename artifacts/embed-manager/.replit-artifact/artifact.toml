kind = "web"
previewPath = "/"
title = "Embed Manager"
version = "1.0.0"
id = "artifacts/embed-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19958

[services.development]
run = "pnpm --filter @workspace/embed-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/embed-manager", "run", "build" ]
publicDir = "artifacts/embed-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19958"
BASE_PATH = "/"
